import Data from '../data/data.js'

var ShortLocationsReducer = (state = Data.shortLocations, actions) => {

    var newState = Object.assign({}, state);

    return state;
}

export default ShortLocationsReducer
